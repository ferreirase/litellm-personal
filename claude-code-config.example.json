{
  "mcpServers": {
    "desktop-commander-http": {
      "url": "http://localhost:8081/mcp",
      "description": "Desktop Commander MCP server via HTTP - provides filesystem, terminal, and process management tools"
    }
  }
}
